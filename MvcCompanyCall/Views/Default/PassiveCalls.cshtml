@using MvcCompanyCall.Models.Entity;
@model List<TblCall>

@{
    ViewBag.Title = "PassiveCalls";
    Layout = "~/Views/Shared/CompanyLayout.cshtml";
}

<table class="table table-bordered">
    <tr>
        <th>ID</th>
        <th>Konu</th>
        <th>Tarih</th>
    
        <th>Detaylar</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.ID</td>
            <td>@item.CallSubject</td>
            <td>@item.CallDate</td>
  
            <td><a hred="#" class="btn btn-info">Detaylar</a></td>
        </tr>
    }
</table>
